import { atom } from "recoil";

export const dropValue = atom({
  key: "dropValue",
  default: "p",
});

export const selectedAction = atom({
  key: "selectedAction",
  default: "p",
});
export const formatSelect = atom({
  key: "selectedAction",
  default: false,
});
